import { Outlet } from 'react-router-dom';

const Auth = () => {
  return (
    <div className="">
        <div className="">
          <Outlet />
        </div>
    </div>
  );
};

export default Auth;
